# ===========================================
# PMT Backend Environment Variables
# ===========================================
# Copy this file to .env and fill in the values

# Database Configuration (Docker)
POSTGRES_DB=project-management-tool
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres

# ===========================================
# Remote Database Configuration (Local profile)
# ===========================================
# For running locally with a remote PostgreSQL database (e.g., AlwaysData)
DATABASE_URL=postgresql-your-instance.alwaysdata.net
DATABASE_PORT=5432
DATABASE_NAME=your_database_name
DATABASE_USERNAME=your_username
DATABASE_PASSWORD=your_password

# ===========================================
# Email Configuration (SMTP)
# ===========================================
# To enable email sending, set MAIL_ENABLED=true
# and configure your SMTP server below

MAIL_ENABLED=false

# Gmail Configuration Example:
# - Enable 2FA on your Google account
# - Create an "App Password" at https://myaccount.google.com/apppasswords
# - Use the 16-character app password (without spaces)
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=test@gmail.com
MAIL_PASSWORD=mypassword

# Sender email address
MAIL_FROM=noreply@pmt.com